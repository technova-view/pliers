{
  "name": "pliers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "core:install": "npm install --prefix core",
    "client:install": "npm install --prefix client",
    "core:dev": "npm run dev --prefix core",
    "client:dev": "npm run dev --prefix client",
    "core:build": "npm run build --prefix core",
    "client:build": "npm run build --prefix client",
    "core:start": "npm run start --prefix core",
    "client:start": "npm run start --prefix client"
  }
}
